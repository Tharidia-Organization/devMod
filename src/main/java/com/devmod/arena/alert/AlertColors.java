package com.devmod.arena.alert;

import com.devmod.shared.SharedColorTokens;

/**
 * Color palette for alert channels (Discord embeds).
 */
public final class AlertColors {
    public static final int ERROR = SharedColorTokens.Alert.ERROR;
    public static final int WARN = SharedColorTokens.Alert.WARN;
    public static final int INFO = SharedColorTokens.Alert.INFO;

    private AlertColors() {}
}

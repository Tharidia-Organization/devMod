# Build properties for DevMod Arena
# These values are populated at build time via gradle task

# Git information
git.commit=${git.commit}
git.branch=${git.branch}

# Build information
build.time=${build.time}
mod.version=${mod.version}

# Environment
build.environment=${build.environment}
